# Secrets — NEVER commit
.env
.env.local
.env.*.local
.db-credentials.local.json
.vercel

# opencode config holds API keys in plaintext
opencode.json
opencode.jsonc
.mcp.json

# Build artifacts
dist/
*.tsbuildinfo

# Dependencies
node_modules/

# Local dev SQLite (regenerate with `bun run db:push && bun run seed`).
# NOTE: prisma/empty.db is NOT ignored — it's the committed schema template
# shipped in the npm package for first-run DB initialization.
prisma/slashloop.db

# Logs
*.log
dev.log
server.log

# OS / editor
.DS_Store
.vscode/
.idea/

# MCP client temp files
.mcp-cache/

# Third-party Supabase agent skills (fetched by opencode/claude, not project source)
.agents/skills/supabase/
.agents/skills/supabase-postgres-best-practices/
.claude/skills/supabase/
.claude/skills/supabase-postgres-best-practices/
